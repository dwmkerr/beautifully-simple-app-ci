language: android
android:
  components:
    - build-tools-19.1.0
    - android-23
before_install:
  - sudo apt-get install jq
  - npm install -g npm cordova ionic
install:
  - cd 2_ionic_app
  - npm install
script:
  - cd 2_ionic_app
  - make test
  - make build-android
